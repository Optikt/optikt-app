/**
 * Semantic badge variants for the Optikt design system.
 * Maps to tonal container/on-container token pairs in layout.css.
 */
export type BadgeVariant = 'success' | 'warning' | 'error' | 'neutral' | 'info' | 'purple';
